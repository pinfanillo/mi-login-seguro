# Este archivo fuerza a NPM a no usar caché ni dependencias preconstruidas,
# lo cual es necesario para SQLite3 en entornos de despliegue como Render.
# También desactiva el paso de la instalación de scripts si no es estrictamente necesario,
# ayudando a la compatibilidad.

# Desactiva el caché local para forzar una instalación limpia
cache-min=999999999

# Fuerza la reconstrucción de módulos nativos (clave para sqlite3)
build-from-source=true

# Desactiva la verificación de la integridad
nocheck=true